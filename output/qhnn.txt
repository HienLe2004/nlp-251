# Sentence 1
intent(order)
order_item(item:phở bò, quantity:2)
deliver_time(giao lúc 12)

# Sentence 2
intent(ask_availability)
ask_availability(item:bún chả)

# Sentence 3
intent(order)
order_item(item:trà sữa, quantity:1)

# Sentence 4
intent(cancel_item)
cancel_item(item:gà rán)

# Sentence 5
intent(ask_price)
ask_price(item:phở bò)

# Sentence 6
intent(ask_menu)

# Sentence 7
intent(ask_my_order)

# Sentence 8
intent(order)
order_item(item:cơm tấm, quantity:1)

# Sentence 9
intent(cancel_item)
cancel_item(item:gà rán)

# Sentence 10
()

# Sentence 11
intent(modify_item)
modify_item(item:phở bò, quantity:3)

# Sentence 12
intent(ask_my_order)
